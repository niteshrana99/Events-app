import { Paper, Avatar } from '@mui/material';
import { styled } from '@mui/system';

export const SignUpPaper = styled(Paper)({
    padding: 20,
    height: '80vh',
    width:400,
    margin: '20px auto'
});

export const SignUpAvatar = styled(Avatar)({
    backgroundColor: '#83aeec'
})