import { IAction } from "../../types/utils.types";

export const adminReducer = (state = null, action: IAction) => {
    switch(action.type) {
        case 'FETCH_EVENTS_DATA':
            return null
    }
}